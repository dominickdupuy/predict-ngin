# Prediction Market Trading — Complete Dependencies

# ── High-granularity data & PMXT integration ──
pmxt>=0.2.0  # Unified API for Polymarket, Kalshi, etc.
websockets>=12.0  # CLOB real-time WebSocket streaming
aiohttp>=3.9.0  # Async HTTP for PMXT
asyncio-contextmanager>=1.0.0  # Async context helpers

# ── Core data processing ──
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0  # Parquet I/O (critical for tick data)
polars>=0.20.0  # Optional: faster columnar ops

# ── API fetching ──
requests>=2.28.0

# ── Statistical & ML ──
scipy>=1.10.0
scikit-learn>=1.3.0
# sentence-transformers>=2.2.0  # Optional: for semantic matching

# ── Backtesting & reporting ──
quantstats>=0.0.62

# ── Config ──
PyYAML>=6.0.1

# ── Visualization ──
matplotlib>=3.7.0
seaborn>=0.12.0

# ── Optional: Notebooks & experimentation ──
# papermill>=2.4.0
# nbformat>=5.7.0
# nbconvert>=7.0.0
# jupyter>=1.0.0
